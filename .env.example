# Required for API: MongoDB (chat history, colleagues, schedules).
# MONGO_URI=mongodb://localhost:27017

# Optional: Qdrant for mem0 vector memory. OpenAI API key and models are set in Settings UI only.
# QDRANT_URL=http://localhost:6333

# Optional: override API port (default 3000).
# PORT=3000

# Optional: API base URL for the web app's Vite dev server proxy (/api, /health). Used when running the web app locally; default is http://localhost:3000. In Docker dev, set to the API service URL (e.g. http://api-dev:3000).
# VITE_PROXY_TARGET=http://localhost:3000

# Optional: working directory for stdio MCP and default filesystem server. In Docker this is set to /app/mcp-cwd; for local API dev set a path that exists on your machine (e.g. ./mcp-cwd).
# MCP_STDIO_DEFAULT_CWD=./mcp-cwd
